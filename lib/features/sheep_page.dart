import 'package:flutter/material.dart';

class SheepPage extends StatelessWidget {
  const SheepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Data Domba"));
  }
}